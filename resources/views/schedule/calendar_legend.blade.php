<div id="legend-container" class="flex justify-center">
    <div class="legend reservable p-2 mr-2 border-solid border-2 rounded-md">{{__('jongman.reservable') }}</div>
	<div class="legend unreservable p-2 mr-2 border-solid border-2 rounded-md">{{ __('jongman.unreservable') }}</div>
	<div class="legend reserved p-2 mr-2 border-solid border-2 rounded-md">{{ __('jongman.reserved') }}</div>
	<div class="legend reserved mine p-2 mr-2 border-solid border-2 rounded-md">{{__('jongman.mine') }}</div>
	<div class="legend reserved participating p-2 mr-2 border-solid border-2 rounded-md">{{ __('jongman.participating') }}</div>
	<div class="legend reserved pending p-2 mr-2 border-solid border-2 rounded-md">{{__('jongman.pending')}}</div>
	<div class="legend pasttime p-2 mr-2 border-solid border-2 rounded-md">{{ __('jongman.past') }}</div>
	<div class="legend restricted p-2 mr-2 border-solid border-2 rounded-md">{{ __('jongman.restricted') }}</div>	
</div>
<div id="legend-container">
    <div class="legend reservable">{{__('jongman.reservable') }}</div>
	<div class="legend unreservable">{{ __('jongman.unreservable') }}</div>
	<div class="legend reserved">{{ __('jongman.reserved') }}</div>
	<div class="legend reserved mine">{{__('jongman.my_reservation') }}</div>
	<div class="legend reserved participating">{{ __('jongman.participating') }}</div>
	<div class="legend reserved pending">{{__('jongman.pending')}}</div>
	<div class="legend pasttime">{{ __('jongman.past') }}</div>
	<div class="legend restricted">{{ __('jongman.restricted') }}</div>	
</div>